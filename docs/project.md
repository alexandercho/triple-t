# Project Overview

This is a boilerplate Fastify + tRPC backend with an Expo frontend.

Current scope:

- Backend serves tRPC procedures.
- Backend includes Prisma and a local PostgreSQL service definition.
- Frontend reads backend data through tRPC.
- Root scripts start both apps for local development.
